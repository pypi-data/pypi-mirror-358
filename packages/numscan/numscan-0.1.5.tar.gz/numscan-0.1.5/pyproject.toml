[project]
name = "numscan"
version = "0.1.5"
description = "Count interesting number patterns in text files"
authors = [
  { name = "Christian Rabenstein" }
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"