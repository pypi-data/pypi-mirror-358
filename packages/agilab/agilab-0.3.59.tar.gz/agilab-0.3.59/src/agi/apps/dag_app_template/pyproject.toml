[project]
name = "your_dag_project"
version = "0.3.59"
description = ""
readme = "README.md"
requires-python = ">=3.11"
authors = [
    {name = "your name", email = "your email"}
]

dependencies = [
 "setuptools",
    "cython",
    "parso",
    "py7zr",
    "python-dotenv",
    "ipython",
    "psutil",
]

[dependency-groups]
dev = [
    "black",
    "pytest",
    "licensecheck",
    "your_dag_project",
]

[project.optional-dependencies]
agi-worker = []
dag-worker = []
workers = [
    "agi-core[dag-worker]",
    "agi-core[agi-worker]"]
managers = [
    "agi-core[managers]"
]


# do not remove
[tool.uv.sources]
agi-cluster = {path = "/path/to/agilab/src/agilab/fwk/cluster", editable = true}

# do not remove
[tool.uv.sources.agi-cluster]
path = "/path/to/agilab/fwk/cluster"
editable = true


[tool.uv.sources.your_dag_project]
workspace = true

[tool.setuptools]
include-package-data = true

[tool.setuptools.data-files]
pyprojects = [
    "src/*.ipynb",
    "src/*.json",
    "src/*.toml",
    "src/*/*.py*",
]

[tool.mypy]
mypy_path = [
    "stubs",
]
disallow_untyped_defs = false
ignore_missing_imports = true
