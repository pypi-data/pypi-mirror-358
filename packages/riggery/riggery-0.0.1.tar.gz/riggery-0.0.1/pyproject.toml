# pyproject.toml for riggery (Namespace Root Placeholder)

[build-system]
requires = ["setuptools >= 61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "riggery"
version = "0.0.1"
authors = [
  { name="Kimon Matara", email="kimon@riggery.com" },
]
description = "Riggery for Maya (top level namespace placeholder)"
readme = "README.md"
requires-python = ">=3.10.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent"
]
license = "MIT"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["riggery"]