[project]
name = "pyfrogsay"
version = "1.0.1"
description = "A talking frog in python inspired by pycowsay"
readme = "README.md"
dependencies = []
requires-python = ">=3.0"

[project.scripts]
pyfrogsay = "pyfrogsay.__main__:main"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"