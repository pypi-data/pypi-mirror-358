[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "openwrapper"
version = "0.1.0"
description = "Discord API wrapper"
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "dataopen"}]
dependencies = ['requests']

[project.urls]
"Homepage" = "https://github.com/dataopen/openwrapper"
"Issues" = "https://discord.gg/opening"