# Simple test for kukula (length) function
onetsa("=== Testing kukula() - Length Function ===")

# Test with lists
ika mylist = ndandanda("apple", "banana", "cherry")
ika size = kukula(mylist)
onetsa("List size:", size)

# Test with strings
ika text = "Hello"
ika length = kukula(text)
onetsa("Text length:", length)

onetsa("=== kukula() working! ===")

