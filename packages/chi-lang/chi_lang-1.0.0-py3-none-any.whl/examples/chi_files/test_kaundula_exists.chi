# Test if kaundula function exists

# Try calling kaundula with no arguments first
ika empty = ndandanda()
onetsa("Empty list works:", empty)

# Now try empty kaundula - this might show if it's registered
onetsa("Testing kaundula...")
onetsa("Type of kaundula:", mtundu(kaundula))

