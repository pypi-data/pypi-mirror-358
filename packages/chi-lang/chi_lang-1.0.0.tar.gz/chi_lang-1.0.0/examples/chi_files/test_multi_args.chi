# Test existing function with multiple arguments

# Test ndandanda (list) which should work with multiple args
ika mylist = ndandanda("a", "b", "c")
onetsa("List test:", mylist)

# Test onetsa with multiple args
onetsa("Multiple", "arguments", "test")

