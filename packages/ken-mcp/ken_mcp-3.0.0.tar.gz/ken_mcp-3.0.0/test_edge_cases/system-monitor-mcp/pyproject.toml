[project]
name = "system-monitor-mcp"
version = "0.1.0"
description = "MCP server for: Create an MCP that monitors system resources (CPU, memory, disk, network), scrapes cryptocurrency pr..."
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["fastmcp>=0.1.0", "psutil", "aiohttp", "websockets", "plotly", "jwt", "aiosqlite", "python-dotenv", "asyncio", "authlib", "beautifulsoup4", "ccxt", "httpx", "lxml", "oauthlib", "openpyxl", "pandas", "psycopg2", "pymysql", "requests", "sqlalchemy", "yfinance"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
