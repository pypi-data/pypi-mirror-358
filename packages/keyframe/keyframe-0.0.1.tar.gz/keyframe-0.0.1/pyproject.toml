[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "keyframe"
version = "0.0.1"
dependencies = [
  "Pillow"
]
requires-python = ">=3.6"
authors = [
  {name = "Mohammad Parsa Mortazavi", email = "mpmms@proton.me"}
]
description = "An animation engine for Python!"
readme = "README.md"
license = {text="MIT"}
keywords = ["animation", "timeline", "keyframe", "graphic"]

[project.urls]
Homepage = "https://github.com/KeyFramePy/Keyframe"