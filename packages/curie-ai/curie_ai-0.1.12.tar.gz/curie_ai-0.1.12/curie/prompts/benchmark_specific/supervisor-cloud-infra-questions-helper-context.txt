AWS Cloud infrastructure related additional context:
- Make sure your experimental plan is valid and grounded in real, accurate facts. When unsure, you can use the AWS CLI package (via the "aws" command) through the "execute_shell_command" tool to query factual information about AWS resources (e.g., VM types and details), to ensure this. 