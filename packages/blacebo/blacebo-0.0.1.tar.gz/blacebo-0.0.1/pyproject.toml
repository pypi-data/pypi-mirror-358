[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[project]
name = "blacebo"
version = "0.0.1"
authors = [
    { name = "Andreas Rühl", email = "blacebo@mail.knusel.me" },
]
description = "A elastic API wrapper."
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "Apache-2.0"
license-files = ["LICEN[CS]E*"]

[project.urls]
Homepage = "https://github.com/aruehl/blacebo"
Issues = "https://github.com/aruehl/blacebo/issues"
