[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agentauth-sdk"
version = "0.0.1-dev0"
description = "Reserved project name for future AgentAuth SDK."
readme = "README.md"
requires-python = ">=3.8"
license = { file = "LICENSE" }
authors = [{ name = "AgentAuth", email = "developers@agentauth.co" }]
keywords = [
    "agentauth",
    "agentauth-id",
    "authentication",
    "identity",
    "self-authenticating",
    "uuid",
    "mcp",
    "mcp-servers",
    "sdk",
    "ai-agents",
    "model-context-protocol"
]
classifiers = [
    "Development Status :: 1 - Planning",
    "Programming Language :: Python :: 3",
    "License :: Other/Proprietary License"
]

[tool.hatch.build]
include = ["agentauth_sdk", "README.md", "LICENSE", "pyproject.toml"]

[project.urls]
"Homepage" = "https://agentauth.co"