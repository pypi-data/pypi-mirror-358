[tool.poetry]
name = "event_scraper_generics"
version = "0.1.1"
description = "All the abstract classes and types used by the event scraper engine."
authors = ["Ezequiel Valencia <ezq.valencia@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
