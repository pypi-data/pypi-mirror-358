[project]
name = "fastapi-reloader"
version = "1.1.1"
readme = "README.md"
dependencies = [
    "fastapi~=0.115",
]

[project.urls]
Homepage = "https://github.com/promplate/hmr"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool]
