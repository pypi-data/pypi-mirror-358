[pytest]
filterwarnings =
    error
    ignore::UserWarning
    ignore::DeprecationWarning

addopts =
    -n=logical
    --maxfail=1
    --cov-config=pytest.coveragerc
    --cov-report=term-missing
    --cov-report=html
    --cov-branch
