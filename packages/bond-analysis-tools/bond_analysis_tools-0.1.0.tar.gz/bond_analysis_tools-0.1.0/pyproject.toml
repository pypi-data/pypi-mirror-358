[project]
name = "bond_analysis_tools"
version = "0.1.0"
description = "A toolset for convertible bond arbitrage analysis using akshare and mcp-sdk."
requires-python = ">=3.10"
dependencies = [
    "akshare",
    "mcp",
    "fastapi",
    "uvicorn[standard]",
    "pandas",
    "lru-cache"
]