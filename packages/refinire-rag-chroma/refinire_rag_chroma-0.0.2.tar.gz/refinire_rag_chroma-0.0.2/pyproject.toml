[project]
name = "refinire-rag-chroma"
version = "0.0.2"
description = "ChromaDB VectorStore plugin for refinire-rag"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "chromadb>=0.4.0",
    "numpy>=2.2.6",
    "oneenv>=0.3.1",
    "pydantic>=2.0.0",
    "refinire-rag>=0.1.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0"
]

[project.entry-points."refinire_rag.vectorstores"]
chroma = "refinire_rag_chroma.chroma_vector_store:ChromaVectorStore"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = [
    "--import-mode=importlib",
    "--cov=src",
    "--cov-report=term-missing"
]
