[project]
name = "tech-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "haobo.jiang", email = "haobo.jiang@transwarp.io" }
]
requires-python = ">=3.10.11"
dependencies = [
    "mcp[cli]>=1.10.1",
]

[project.scripts]
tech-mcp-demo = "tech_mcp_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
