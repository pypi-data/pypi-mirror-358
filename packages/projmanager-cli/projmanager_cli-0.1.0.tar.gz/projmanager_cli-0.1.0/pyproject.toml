[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "projmanager-cli"
version = "0.1.0"
description = "All-in-one project manager CLI tool"
authors = [{name = "kkyian", email = "kkyian@icloud.com"}]
dependencies = ["click", "requests"]
requires-python = ">=3.7"

[project.scripts]
projman = "projman:cli"
