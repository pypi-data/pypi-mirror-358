[project]
name = "prototuner"
version = "0.1.1"
description = "Placeholder for Prototuner to avoid takeovers"
readme = "README.md"
authors = [{ name = "All Tuner Labs", email = "hey@alltuner.com" }]
requires-python = ">=3.9"
dependencies = []


[build-system]
requires = ["uv_build>=0.7,<0.8"]
build-backend = "uv_build"
