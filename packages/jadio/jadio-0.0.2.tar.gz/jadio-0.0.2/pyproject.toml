[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "jadio"
dynamic = ["version"]
description = "Modular CLI framework - Node.js style package manager for Python"
readme = "README.md"
license = "MIT"
requires-python = ">=3.8"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "requests",
    "rich",  # for nice CLI output
]

[project.urls]
Homepage = "https://pypi.org/project/jadio/"
Repository = "https://github.com/JaxxyJadio/jadio"

[project.scripts]
jadio = "jadio.cli.main:main"

[tool.hatch.version]
path = "src/jadio/__init__.py"

[tool.hatch.build.targets.sdist]
include = [
    "/src",
    "/README.md",
    "/LICENSE",
]

[tool.hatch.build.targets.wheel]
packages = ["src/jadio"]