[build-system]
requires = ["hatchling == 1.27.0"]
build-backend = "hatchling.build"

[project]
name = "structest"
version = "0.0.1"
readme = "README.md"

[project.scripts]
structest = "structest:main"
