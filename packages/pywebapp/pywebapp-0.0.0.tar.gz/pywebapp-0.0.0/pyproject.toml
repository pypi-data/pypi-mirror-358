[project]
name = "pywebapp"
version = "0.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.3",
    "pyinstaller>=6.14.1",
    "pywebview>=5.4",
    "typer>=0.16.0",
    "watchdog>=6.0.0",
]


[build-system]
requires = ["setuptools>=67.7.2", "wheel>=0.42.0"]
build-backend = "setuptools.build_meta"
