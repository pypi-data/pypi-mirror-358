[project]
authors = [{name = "ControlNet", email = "smczx@hotmail.com"}]
name = "naver"
requires-python = ">= 3.11"
version = "0.0.0"
dependencies = ["build>=1.2.2.post1,<2", "twine>=6.1.0,<7"]

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
naver = { path = ".", editable = true }

[tool.pixi.tasks]
