FROM {{ config.pps_base_image }}
{# A hack to make it so our integration tests work w/o having to use DRUM or PPS #}
{% if "ealen/echo-server" in config.pps_base_image %}
ENV PORT=8080
{% endif %}
COPY model_{{ di.model_id }}.mlpkg /opt/ml/model/
LABEL com.datarobot.ppsVersion={{ config.pps_version }}
