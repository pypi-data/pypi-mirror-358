{% import '_helpers.j2' as helpers %}
apiVersion: v1
kind: Secret
metadata:
  name: "{{ helpers.name(di) }}"
  labels:
    {{ COMPONENT_LABEL_NAME }}: predictionServer
    {{ helpers.default_labels(di) | indent(4) }}
  annotations:
    {{ helpers.default_annotations() | indent(4) }}
type: Opaque
data:
  secret: {{ config.datarobot_api_key | b64encode }}
