[project]
name = "xl-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.10.1",
]

[project.scripts]
xl-mcp-demo = "xl_mcp_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
