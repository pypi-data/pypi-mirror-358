[project]
name = "kndrpy"
version = "0.1.0"
description = "Intelligent document parsing and inference engine"
readme = "README.md"
authors = [{ name = "BSAM", email = "bsamuel@amalgusglobal.com" }]

[build-system]
requires = ["setuptools>=61.0,<70", "wheel"]
build-backend = "setuptools.build_meta"
