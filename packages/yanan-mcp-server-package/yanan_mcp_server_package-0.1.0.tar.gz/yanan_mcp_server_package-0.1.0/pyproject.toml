[project]
name = "yanan-mcp-server-package"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "彭亚楠", email = "pengyanan1128@126.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.9.4",
]

[project.scripts]
yanan-mcp-server-package = "yanan_mcp_server_package:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
