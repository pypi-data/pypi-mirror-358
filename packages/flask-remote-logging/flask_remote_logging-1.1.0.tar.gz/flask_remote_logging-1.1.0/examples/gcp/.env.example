# Google Cloud Logging Flask Example Environment Variables
# Copy this file to .env and adjust the values for your setup

# Google Cloud Configuration
GCP_PROJECT_ID=your-gcp-project-id
# GCP_CREDENTIALS_PATH=/path/to/service-account.json  # Optional if using default credentials

# Cloud Logging Configuration
GCP_LOG_NAME=flask-gcp-example
GCP_LOG_LEVEL=INFO
GCP_ENVIRONMENT=development

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=true
PORT=5000

# Optional: Custom user simulation with GCP context
# These headers can be used to simulate different GCP users in requests
# Example: curl -H "X-User-ID: alice123" -H "X-GCP-User-Email: alice@example.com" http://localhost:5000/logs/user-context
