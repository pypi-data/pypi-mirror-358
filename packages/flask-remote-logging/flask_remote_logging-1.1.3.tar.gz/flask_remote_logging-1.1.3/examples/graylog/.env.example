# Graylog Flask Example Environment Variables
# Copy this file to .env and adjust the values for your setup

# Graylog Server Configuration
GRAYLOG_HOST=localhost
GRAYLOG_PORT=12201
GRAYLOG_LEVEL=INFO
GRAYLOG_ENVIRONMENT=development

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=true

# Optional: Custom user simulation
# These headers can be used to simulate different users in requests
# Example: curl -H "X-User-ID: alice123" -H "X-Username: alice" http://localhost:5000/logs/user-context
