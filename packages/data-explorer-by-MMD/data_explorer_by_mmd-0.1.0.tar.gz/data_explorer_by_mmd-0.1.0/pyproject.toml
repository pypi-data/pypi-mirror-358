[project]
name = "data_explorer_by_MMD"
version = "0.1.0"
description = "Package pour explorer les données économiques de la Banque Mondiale"
readme = "README.md"
requires-python = ">=3.7"
authors = [
    {name = "Mamadou Moustapha DIALLO", email = "mmdialloisidk1@groupeisi.com"}
]
license = {text = "MIT"}
dependencies = [
    "pandas",
    "requests"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"