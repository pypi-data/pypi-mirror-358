'use client'
import {model_name}Detail from "@/modules/{app_file_name}/{model_file_name}/components/{model_file_name}.detail";
import {{ useParams }} from "next/navigation";

export default function {model_name}(){{
    const params = useParams()
    const id = params.{model_file_name}_id as string

    return <{model_name}Detail id={{id}} />
}}