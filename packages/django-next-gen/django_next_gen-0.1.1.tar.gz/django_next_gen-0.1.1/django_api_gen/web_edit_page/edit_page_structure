'use client'
import {model_name}Form from "@/modules/{app_file_name}/{model_file_name}/components/{model_file_name}.form";
import {{ useParams }} from "next/navigation";

export default function {model_name}Edit(){{
    const params = useParams()
    const id = params.{model_file_name}_id as string

    return <{model_name}Form id={{id}} />
}}