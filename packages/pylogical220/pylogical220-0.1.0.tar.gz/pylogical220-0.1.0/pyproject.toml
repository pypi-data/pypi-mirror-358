[project]
name = "pylogical220" 
version = "0.1.0"
description = "Useful package for logic of python, contains 18 attributes."
authors = [{ name = "as-220" }]
license = { file = "LICENSE" }
readme = "README.md"
requires-python = ">=3.7"
dependencies = []

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
