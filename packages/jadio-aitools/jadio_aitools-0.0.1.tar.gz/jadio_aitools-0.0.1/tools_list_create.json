{
  "create_html_file": {
    "description": "Creates an HTML file with optional title.",
    "required": ["--output"],
    "optional": ["--title", "--json"],
    "example": "python create_html_file.py --output index.html --title \"Landing Page\""
  },
  "create_python_file": {
    "description": "Creates a Python file with starter code.",
    "required": ["--output"],
    "optional": ["--name", "--json"],
    "example": "python create_python_file.py --output script.py --name \"MyScript\""
  },
  "create_markdown_file": {
    "description": "Creates a Markdown (.md) file with optional title.",
    "required": ["--output"],
    "optional": ["--title", "--json"],
    "example": "python create_markdown_file.py --output README.md --title \"Project Title\""
  },
  "create_css_file": {
    "description": "Creates a CSS file with starter style rules.",
    "required": ["--output"],
    "optional": ["--name", "--json"],
    "example": "python create_css_file.py --output styles.css --name \"MainStyles\""
  },
  "create_json_file": {
    "description": "Creates a JSON file with a starter object.",
    "required": ["--output"],
    "optional": ["--name", "--json"],
    "example": "python create_json_file.py --output config.json --name \"AppConfig\""
  },
  "create_text_file": {
    "description": "Creates a plain text (.txt) file with optional content.",
    "required": ["--output"],
    "optional": ["--content", "--json"],
    "example": "python create_text_file.py --output notes.txt --content \"My notes here.\""
  },
  "create_license_file": {
    "description": "Creates a LICENSE file with type, holder, and year.",
    "required": ["--output"],
    "optional": ["--type", "--holder", "--year", "--json"],
    "example": "python create_license_file.py --output LICENSE --type MIT --holder \"Alice\" --year 2024"
  },
  "create_gitignore_file": {
    "description": "Creates a .gitignore file with a language template.",
    "required": ["--output"],
    "optional": ["--template", "--json"],
    "example": "python create_gitignore_file.py --output .gitignore --template Python"
  },
  "create_javascript_file": {
    "description": "Creates a JavaScript (.js) file with starter function.",
    "required": ["--output"],
    "optional": ["--name", "--json"],
    "example": "python create_javascript_file.py --output app.js --name \"MyApp\""
  },
  "create_php_file": {
    "description": "Creates a PHP file with starter function.",
    "required": ["--output"],
    "optional": ["--name", "--json"],
    "example": "python create_php_file.py --output index.php --name \"WebApp\""
  },
  "create_rust_file": {
    "description": "Creates a Rust (.rs) file with starter main function.",
    "required": ["--output"],
    "optional": ["--name", "--json"],
    "example": "python create_rust_file.py --output main.rs --name \"CLIApp\""
  }
}
