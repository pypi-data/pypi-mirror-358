[project]
name = "repository-management-api"
version = "0.0.1"
description = "A FastAPI service for creating and managing Git repositories."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest==8.*",
    "asgi-lifespan",
    "fastapi[all]",
    "cognito-jwt-verifier",
]
