import { createApp } from "vue";
import StreamlitCodeDiff from "./StreamlitCodeDiff.vue";
import "./index.css";

// Create and mount Vue app
const app = createApp(StreamlitCodeDiff);
app.mount("#root");
