[project]
name = "tenmao-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kuanglong2016", email = "kuanglong2016@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.10.1",
]

[project.scripts]
tenmao-mcp-demo = "tenmao_mcp_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
