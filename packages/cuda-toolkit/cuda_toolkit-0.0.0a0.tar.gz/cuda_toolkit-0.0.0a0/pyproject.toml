[project]
name = 'cuda-toolkit'
authors = [
    {name = 'NVIDIA Kitmaker Team', email = 'compute_installer@nvidia.com' },
]
description = 'Zero version placeholder for cuda-toolkit'
license = {text = 'Apache-2.0'}
dependencies = []
version = '0.0.0a0'

[build-system]
requires = ['hatchling']
build-backend = 'hatchling.build'
