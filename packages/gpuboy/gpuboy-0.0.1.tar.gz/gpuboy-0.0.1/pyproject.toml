[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gpuboy"
version = "0.0.1"
description = "Fast GameBoy emulation on GPUs"
readme = "README.md"
authors = [{name = "Jonas Eschmann", email = "jonas.eschmann@gmail.com"}]
license = {text = "MIT"}
dependencies = [
]

[tool.hatch.build.targets.wheel]
packages = ["gpuboy"]

