[project]
name = "shapes-overlay"
version = "0.1.0"
description = "Operações espaciais entre shapefiles com GeoPandas overlay"
authors = [{ name = "Ricardo Malnati", email = "ricardomalnati@gmail.com" }]
license = "MIT"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "geopandas",
    "shapely",
    "fiona",
    "pyproj"
]

[project.scripts]
shapes-overlay = "overlay:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["shapes_overlay"]