[project]
name = "worldbank_api"
version = "0.1.0"
description = "Analyse des données économiques via l’API World Bank"
authors = [{ name="Diallo", email="saikoudiallo125@gmail.com" }]
dependencies = ["requests", "pandas"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
