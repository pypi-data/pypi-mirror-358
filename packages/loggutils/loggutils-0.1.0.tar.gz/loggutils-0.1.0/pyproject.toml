[project]
name = "loggutils"
version = "0.1.0"
description = "Утилиты для логгирования"
authors = [{ name = "Michael", email = "stroytell049@gmail.com" }]
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["loggutils"]