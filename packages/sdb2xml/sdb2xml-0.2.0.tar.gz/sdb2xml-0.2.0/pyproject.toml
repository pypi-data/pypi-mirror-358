[project]
name = "sdb2xml"
version = "0.2.0"
description = "Dump SDB file to xml using apphelp.dll"
readme = "README.md"
license = "MIT"
authors = [
    { name = "Mark Jansen", email = "mark.jansen@reactos.org" }
]
requires-python = ">=3.10"
dependencies = [
    "click>=8.2.1",
]
keywords = ["sdb", "sdb2xml", "shim", "apphelp", "appcompat", "shimeng"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Programming Language :: Python :: 3",
    "Operating System :: Microsoft :: Windows",
    "Topic :: Utilities",
]

[project.urls]
Homepage = "https://pypi.org/project/sdb2xml/"
Changelog = "https://github.com/learn-more/sdb2xml/releases"
Issues = "https://github.com/learn-more/sdb2xml/issues"
CI = "https://github.com/learn-more/sdb2xml/actions"
Repository = "https://github.com/learn-more/sdb2xml"

[project.scripts]
sdb2xml = "sdb2xml.cli:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.sdist]
exclude = [
  ".vscode",
  ".python-version",
  "uv.lock",
  "tests"
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]

